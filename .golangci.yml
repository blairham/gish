# golangci-lint v2 configuration for gish
# See: https://golangci-lint.run/usage/configuration/

version: "2"

run:
  timeout: 10m
  tests: true
  modules-download-mode: readonly

output:
  formats:
    text:
      print-linter-name: true
      print-issued-lines: true
      colors: true

linters:
  enable:
  - errcheck
  - govet
  - ineffassign
  - staticcheck
  - unused
  - misspell
  - unconvert
  - gocritic
  - revive

  exclusions:
    paths:
    # protoc output; regenerate with `make proto` instead of editing
    - pkg/pluginapi

    rules:
    - path: _test\.go
      linters:
      - errcheck
      - gocritic
      - revive

    - path: cmd/
      linters:
      - errcheck

  settings:
    misspell:
      locale: US

    errcheck:
      check-type-assertions: false
      check-blank: false
      exclude-functions:
      # Prompt writes to the session's own stdout/stderr — failure is unactionable
      - fmt.Fprint
      - fmt.Fprintf
      - fmt.Fprintln
      - os.Setenv
      - os.Unsetenv
      - os.Remove
      - os.RemoveAll
      - os.MkdirAll
      - os.WriteFile
      - (os.File).Close
      - (*os.File).Close
      - (io.ReadCloser).Close
      - (*compress/gzip.Reader).Close
      - (*archive/zip.ReadCloser).Close
      - (net.Conn).Close
      - (net.Listener).Close
      - (*google.golang.org/grpc.ClientConn).Close
      - (*os/exec.Cmd).Run

    govet:
      disable:
      - shadow

    gocritic:
      enabled-tags:
      - diagnostic
      - performance
      disabled-checks:
      - hugeParam
      - rangeValCopy
      - ifElseChain
      - commentedOutCode
      - appendCombine

    revive:
      severity: warning
      rules:
      - name: blank-imports
      - name: context-as-argument
      - name: error-return
      - name: error-strings
      - name: error-naming
      - name: if-return
      - name: increment-decrement
      - name: var-naming
      - name: var-declaration
      - name: range
      - name: receiver-naming
      - name: unexported-return
      - name: indent-error-flow
      - name: errorf
      - name: superfluous-else
      - name: unreachable-code

formatters:
  enable:
  - gofumpt
  - goimports

  settings:
    goimports:
      local-prefixes:
      - github.com/blairham/gish

  exclusions:
    paths:
    - pkg/pluginapi

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
